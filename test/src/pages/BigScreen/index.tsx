import Viewer from '@/components/Viewer';
import styles from './index.module.scss';

const { baseMap } = constants;
function BigScreen() {
  return <div className={styles['screen-container']}>daping</div>;
}

export default BigScreen;
